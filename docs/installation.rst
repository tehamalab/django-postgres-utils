============
Installation
============

At the command line::

    $ easy_install django-postgres-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-postgres-utils
    $ pip install django-postgres-utils
